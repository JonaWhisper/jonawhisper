[package]
name = "jona-engine-voxtral"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
inventory = "0.3"
jona-types = { path = "../jona-types" }
jona-engines = { path = "../jona-engines" }
log = "0.4"

[build-dependencies]
cc = "1"
